<!--index.wxml-->
<view class="index">
  <view class="index-profile">
     <view wx:if="{{ profile.avatarUrl }}" class="index-profile__img" style="background-image: url('{{profile.avatarUrl}}')"></view>
     <view wx:else class="index-profile__img-hold">暂无头像</view>
  </view>
  <view>
    <view class="index-title">
      {{ title }}
    </view>
    <view>
    {{nick}}
    </view>
  </view>

  <!-- <view class="index-books">
    <view class="index-books__showLayer">
      <view class="index-books__item" wx:for="{{ comments }}" wx:key="{{ index }}">
        <view class="index-books__controls--show" wx:if="{{ !item.isEditing }}">{{ item.comments }} {{item.nick_name}}
        </view>
      </view>
    </view>
  </view> -->
</view>
